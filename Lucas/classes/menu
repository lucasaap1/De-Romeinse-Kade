<?php

class Menu
{
    public function showItems()
    {
        $db = include '../../../De-Romeinse-Kade/main/db/db.php';

        $getItems = $db->prepare("SELECT * FROM items");
        $getItems->execute();

        return $getItems->fetchAll(PDO::FETCH_ASSOC);
    }
}
    // public function addToMenu() {

    // }

    // public function deleteItemFromMenu() {

    // }
// }
